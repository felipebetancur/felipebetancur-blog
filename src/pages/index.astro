---
import { SITE_TITLE, SITE_TAGLINE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';
import Ava from '@src/assets/img/home.png';
import Prose from '../components/Prose.astro';
import { projects } from './projects/projects';
import ProjectList from '../components/ProjectList.astro'
import PostItem from '../components/PostItem.astro';

type BlogPost = CollectionEntry<'blog'>;

const top6Projects = projects.slice(0, 6);
const posts = (await getCollection('blog')).sort((a: BlogPost, b: BlogPost) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()).slice(0, 5);
---

<BaseLayout title={SITE_TITLE + ' - ' + SITE_TAGLINE} description={SITE_DESCRIPTION}>
	<div class='container'>
		<div class='w-full md:flex md:items-center gap-16 mb-10'>
      <div class="text-pretty leading-9">
        <h1 class="text-3xl font-bold mb-4 text-center">Exploraciones en sistemas embebidos, música e innovación</h1>
        <Prose>
          <div class="text-lg">
            Bienvenido a mi espacio digital.
            <p>Soy Felipe Betancur, ingeniero electrónico y músico, dedicado al diseño de sistemas embebidos, hardware, firmware y a la exploración del Internet de las Cosas Musicales (IoMusT). Este blog reúne mis proyectos, análisis y aprendizajes en torno a la ingeniería, el rendimiento en Linux (perf y eBPF) y la creación de experiencias interactivas que integran tecnología, arte y curiosidad técnica.</p>
            <p>Aquí encontrarás desde tutoriales y casos de estudio, hasta reflexiones prácticas sobre documentación técnica en LaTeX y desarrollo web con Astro. Mi propósito es compartir mis exploraciones técnicas y artísticas. Explora, aprende y crea conmigo.</p>
          </div>
        </Prose>
      </div>
      <Image src={Ava} alt="Felipe Betancur" width={300} loading="eager" class="rounded-3xl mx-4 md:mx-0"/>
		</div>
		<main>
      <div class='mt-6 mb-12'>
        <div class="flex justify-between gap-2 border-b mb-1 dark:border-b-zinc-700">
          <h2 class="text-lg font-bold mb-3">Últimas publicaciones</h2>
          <a href="/posts/" class="inline-block py-2 underline dark:prose-invert">Todos los artículos &raquo;</a>
        </div>
        <div>
          {
            posts.map((post: BlogPost) => (
              <PostItem post={post} />
            ))
          }      
        </div>
        <div class="text-right">
        </div>
      </div>
      <div>
        <div class="flex justify-between gap-2 mb-1">
          <h2 class="text-lg font-bold mb-3">Proyectos</h2>
          <a href="/projects/" class="inline-block py-2 underline dark:prose-invert">Todos los proyectos &raquo;</a>
        </div>
        <ProjectList projects={top6Projects}/>
      </div>
		</main>
	</div>
</BaseLayout>
